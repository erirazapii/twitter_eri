<%= form_for(@reply) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field">
    <%= f.text_area :content, placeholder: "reply tweet..." %>
  </div>
  <%= f.submit "tweet", class: "btn btn-primary" %>
<% end %>